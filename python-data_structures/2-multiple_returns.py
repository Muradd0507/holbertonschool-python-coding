#!/usr/bin/python3
def multiple_returns(sentence):
    if sentence != "":
        tup1 = (len(sentence),)
        tup2 = (sentence[0],)
        return tup
    else:
        print("Length: {} -First character: {}".format(len(string), None))
